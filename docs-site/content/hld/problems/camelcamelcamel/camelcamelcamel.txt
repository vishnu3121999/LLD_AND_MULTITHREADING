	• Functional-Requirements:
		○ Users should be able to view price history for Amazon products (via website or Chrome extension) - sliding window
		○ Users should be able to subscribe to price drop notifications with thresholds (via website or Chrome extension)

		○ Out-of-scope:
			§ Search and discover products on the platform
			§ Price comparison across multiple retailers

	• Non-functional Requiements:
		○ No SPOF
		○ Availability > Consistency (eventual consistency)
			§ Notifications within 1hr of price drop
		○ System Scale:
			§ System Size:
				□ 500M products on Amazon + 3k new products everyday
				□ 1M DAU
				□ price changes every 1hr on avg
			§ Throughput:
				□ Read QPS (product history)  = 10^6/(7*10^5)  = 10 /s
				□ Wrtie TPS (adding subscriptions) = 10 /s
			§ Latency:
				□ Read latency:
					®  < 500ms for history view
					® < 1hr for price drop noti


	• Core Entities:
		○ User
		○ Product
		○ Price
		○ Subscriptiom

	• API Design:
		○ GET /products/{product_id}/price?period=30d&granularity=daily -> PriceHistory[]       (daily avg for past 30days; fixed query structure,hence not passing query as param like metrics-monitoring system)
		○ POST /subscriptions
		{
		  product_id,
		  price_threshold,
		  notification_type,
		}
		-> 200

	• HLD:
		○

	• Deep Dives:
		○ Read Latency:
			§ Tables Size:
				□ Price Table:
					® rows: 500M * 24 hr/day * 400 days /year * 10 years = 5*10^13
					® size: rows * 100Bytes = 5*10^15 = 5PB
				□ Subscription Table:
					® rows: 1M users * 1/week * 4 week/month * 12 month/year *10 year = 500M rows
					® size: 500B * 100Bytes = 5 * 10^13 = 50GB
			§ priceHistory < 500ms:  (period=10y&granularity=daily)
				□ current approach (without index) : (BAD)
				□ Indexing: (Good)
					® No of rows = 24 * 400 *10 = 10^5 , Data size = 10^7Bytes  => 10ms
					® Total latency:
						◊ 10ms (data load time) + aggregation time (8ms) + sorting time + network latency will be  < 500ms
							}
				□ Scheduled Pre-aggregation using daily Cron: (Good)
					® Every night run a cronjob that calculates daily, weekly, monthly price summaries and store these in separate tables. To avoid above aggregation latency.
					® Faster than above , but way more storage & 1 day staleness
				□ TSDB : (Great)
					® optimized for aggregations on ts
					® Even faster (10-100x)
					® 10% less storage

			§ noti < 1hr
				□ Crawling:
					® with 1 req/s rate limit, to scrape 500M = 5000 days/vm = 5 days with 1000 VMs (smaller VMs sufficient as only 1req/s)
					® So cant crawl all every hour for sure.
				□ Note: Product popularity follows a Pareto distribution. y=c/x^k
					® Lets consider some numbers for calculations. I remember a stat from yoututbe that 95% of total video views comes from 3% of videos.
					® Instead of treating all 500 million products equally, we can tier our crawling based on actual user interest and dramatically improve our service quality where it matters most.
						◊ 1% products - interestedBy 90%  - time to scrape: 1.5hr
						◊ 9% products - interestedBy 8%  - time to scrape: 15hr
						◊ 90% products - interestedBy 2%  - time to scrape:
					® How to identify high interested products?
						® Based on user subscriptions
						® Based on user interaction on products like search etc
				□ 500M subscriptions rows :
					® for each row check for products price change
					® 500M subscription rows + 500M price rows = 1B total rows to be scanned
					® Data size = 100GB
					® Latency = 2min
				□ Approach-1:
					® So lets run 3 schedulers , each push the products with respective queue
						◊ high priority scheduler runs every 55min, 90% users receive noti within 1hr
						◊ med priority scheduler run every 15 hr , 8% users receive within 15hr
						◊ low priority scheduler run every x days , 8% users receive within x days
					® 3000 VMs is still so big. (why 3000 & not 1000 ?  -  since each priority products need 1000VMs to keep scraping every 1hr,15hr,x hr )
						◊ Can use the extension that users installed while browsing Amazon, to send the updates to our system reg the price of the product and suggested products listed on the page they are viewing. This will add an entry to Price table and also update lastScrapedAt in Product table.
						◊ We do it transparently, asking users to ack when installing the plugin.
						◊ We can discover newly added products also in this way.
						◊ So inorder to make use of this, we need to change the scheduling a bit. Lets consider high priority products, if we only batch poll all high P products at once and send to SQS, we cant detect the scrapes done from extension in any way. So we need the scheduler to run every 1min & check for high P products whose lastScrapedAt > 1hr and only enque those to SQS.
							} So all 3 schedulers, run every 1min, and check for prodcuts whose lastScrapedAt >  (1hr,15hr,xhr)
						◊ This will significantly reduce the number of VMs we need. Autoscale webcrawler based on SQS size

					®
				□ Approach-2:
					® Scheduler runs only to fetch newly added products every 5mins.
					® Products readded back to SQS by changing the visibility timeout
					® Reduces writes on DB
					® Autoscale using SQS size possible? Yes this time using visibleMsgs count.
					® Lets go ahead with this.
					®
					® How do we handle potentially malicious price updates from Chrome extension users?
						◊

						◊

						◊
		○ Write TPS:
			§ look for line arrows in diagram to figure out, not just the write TPS from NFRs
			§ addSubscriptions - 15/s  (very less)
			§ TSDB: updateScrapeTs - with 3k servers - 3k/s   - (TODO - read more on the scalability of TSDBs and which TSDB you want to use- generally 100k/s)
		○ Read QPS:
			§ TSDB read TPS - TODO
			§ 500M postgres reads per hr *2 (noti cron + noti svc) :
				□ Batch poll (BAD)
				□ Cursor based poll (Good)
					® instead of batch requesting all at once (puts lot of load i.e 1B/s for the first sec & zero afterwards) , have the subscriptionId as a monotonic counter & use cursor based polling. And reset every 1hr.
						◊ If pollinterval = 1s, then limit in SQL  = 1B/3600 since prev_cursor.  => peak QPS = 3*10^5/s
						◊ postgres QPS = 100k/s conditions apply ( so can have 3 read replicas)
					® Cons:
						◊ noti delivery to client in worst case 2hr.
							} If it falls one the last minute of crawler poll & last min of cron's poll.
						◊ SPOF (if cron fails in middle, noti gets delayed by more 1hr)
				□ CDC:
					® whenever there is a cron on DB - replace with CDC
					® repalcing polling based approach to event driven
					® realtime notifications
					®
				□ Dual Write:
					® The dual-write approach lets us be smarter about which changes trigger notifications. We can filter out tiny price fluctuations that wouldn't interest users or batch multiple rapid changes before publishing events. We also avoid the overhead of database triggers on every single price update.
					® This also avoids overhead of database triggers & inconsistency is fine here.
					®

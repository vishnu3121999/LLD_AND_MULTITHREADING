# LLD And Multithreading Notes

Use this space for interview-prep notes, design tradeoffs, diagrams, and examples.

## Java Visualizer

Open the Java Visualizer tab, paste a local folder path, and scan. The backend reads `.java` files recursively, removes imports, constructors, getters, and setters, then renders each file as a draggable block.

## Content

Add more markdown files inside `docs-site/content/docs`. Each file appears in the docs navigation after restarting the app.

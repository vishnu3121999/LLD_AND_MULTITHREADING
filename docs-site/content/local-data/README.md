# Local Data

Runtime UI state that used to live in browser localStorage is stored in `browser-state.json`.

The file is created automatically by `/api/local-data` when the app saves workspace preferences, code block layouts, zoom levels, folded methods, theme choices, or demo auth state.

Back up `browser-state.json` if you want to preserve workspace organization across machines.
